import { PipeTransform } from '@angular/core';
import { Observable } from 'rxjs';
import { IsLoadingService, Key } from '../../is-loading.service';
export declare class IsLoadingPipe implements PipeTransform {
    private isLoadingService;
    constructor(isLoadingService: IsLoadingService);
    transform(key: Key): Observable<boolean>;
}
